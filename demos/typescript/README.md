# TypeScript Demo

This demo is the authoring side of the first sandbox proof.

- Author the SemVer pattern with the STRling `Simply` API.
- Export a canonical AST artifact to `../../patterns/semver.ast.json`.
- Validate sample inputs using the installed npm package.

## Commands

```bash
bash bootstrap.sh
npm run build
npm run export
npm run verify
```
